plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.puranamasu45.slime_room"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.puranamasu45.slime_room"
        minSdk = 23
        targetSdk = 35
        versionCode = 1
        versionName = "0.1.0"
    }
}

flutter {
    source = "../.."
}
